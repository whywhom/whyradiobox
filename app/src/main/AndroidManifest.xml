<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.whywhom.soft.whyradiobox">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".RBApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.subscribedetail.SubscribeDetailActivity"></activity>
        <activity
            android:name=".ui.detail.PodcastDetailActivity"
            android:label="@string/title_activity_podcast_detail"></activity>
        <activity
            android:name=".ui.add.AddFeedActivity"
            android:label="@string/app_name"></activity>
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>